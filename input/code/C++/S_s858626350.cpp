#include <bits/stdc++.h> 
using namespace std;
#define rep(i, N) for(int i=0; i<N; i++)
typedef long long ll;
#define dump(x) cerr<<#x<<"="<<x<<endl
using P = pair<int, int>;

int main() {
   char a;
   cin >> a;
   if(isupper(a)){
      cout << 'A' << endl;
   }
   else{
      cout << 'a' << endl;
   }
   return 0;
}
