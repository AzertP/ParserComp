/* ITP1_8_B */


/* ???????????????????????????? */
void init_array(char ch_array[]);
/* ????????????????????????????????????????????????????? */
short is_continue(char ch_array[]);
/* ?????????????????????? */
short get_sum(char ch_array[]);
/* ????????????????????????????????*/
short convert_number(char target);

int main(void)
{
	char ch_array[MAX_DIGIT];
	
	short ii;
	
	while( 1 )
	{
		// ???????????????
		init_array(ch_array);
		
		// ??\????????????
		scanf("%s", ch_array);
		
		// ??????????????????????
		if(0 == is_continue(ch_array))
		{
			break;
		}
		
		// ???????????????????????????
		printf("%hd\n", get_sum(ch_array));
	}
	return 0;
}

/* ???????????????????????????? */
void init_array(char ch_array[])
{
	short ii;
	for( ii = 0; ii < MAX_DIGIT; ii++ )
	{
		ch_array[ii] = '\0';
	}
}

/* ????????????????????????????????????????????????????? */
short is_continue(char ch_array[]){
	if( '0' == ch_array[0] )
	{
		return 0;
	}
	else
	{
		return 1;
	}
}

/* ???????????? */
short get_sum(char ch_array[])
{
	short ii = 0, sum = 0;
	while( 1 )
	{
		if( '\0' == ch_array[ii] )
		{
			break;
		}
		
		sum += convert_number(ch_array[ii]);
		
		ii++;
	}
	return sum;
}

short convert_number(char target)
{
	short ret_num = 0;
	
	if( '0' <= target && '9' >= target )
	{
		ret_num = (short)(target - CONVERT);
	}
	
	return ret_num;
}
